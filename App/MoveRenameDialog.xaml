<Window x:Class="Accel.App.MoveRenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Accel.App.Controls"
        Title="Rename / Move" Height="300" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="None" Style="{DynamicResource DialogWindowStyle}">
    <Window.Resources>
        <ResourceDictionary Source="Theme.xaml" />
    </Window.Resources>
    <!-- Same elevation ladder as RenameSessionDialog: L1 surface body under the L2 title bar. -->
    <Border BorderBrush="{StaticResource WindowBorderBrush}" BorderThickness="1">
    <DockPanel LastChildFill="True" Background="{StaticResource SurfaceBrush}">
        <controls:CustomTitleBar DockPanel.Dock="Top" TitleText="Rename / Move"
                                  ShowMinimizeButton="False" ShowMaximizeButton="False" />
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Current path" Style="{StaticResource FieldLabelTextStyle}" />
            <!-- Read-only but still selectable/copyable, unlike a plain TextBlock. -->
            <TextBox Grid.Row="1" Text="{Binding CurrentPath, Mode=OneWay}"
                     IsReadOnly="True" IsHitTestVisible="True" Focusable="True"
                     Margin="0,0,0,12" />

            <TextBlock Grid.Row="2" Text="New path" Style="{StaticResource FieldLabelTextStyle}" />
            <Grid Grid.Row="3" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewPathBox" Grid.Column="0"
                         Text="{Binding NewPath, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1" Content="Browse…" Command="{Binding BrowseCommand}"
                        Margin="8,0,0,0" MinWidth="88" />
            </Grid>

            <!-- Validation error: an inset danger card, matching RenameSessionDialog's. -->
            <Border Grid.Row="4" Background="#1FE98F8F" BorderBrush="#4DE98F8F" BorderThickness="1"
                    CornerRadius="6" Padding="12,8"
                    Visibility="{Binding Visibility, ElementName=ErrorText}">
                <TextBlock x:Name="ErrorText"
                           Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"
                           Visibility="Collapsed" />
            </Border>

            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="CancelButton" Content="Cancel" Command="{Binding CancelCommand}"
                        MinWidth="96" Margin="0,0,10,0" IsCancel="True" />
                <Button x:Name="ConfirmButton" Content="Move" Command="{Binding ConfirmCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" MinWidth="96" IsDefault="True" />
            </StackPanel>
        </Grid>
    </DockPanel>
    </Border>
</Window>
