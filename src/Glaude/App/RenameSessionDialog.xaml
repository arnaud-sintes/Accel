<Window x:Class="Glaude.App.RenameSessionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename session" Height="160" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Name" FontWeight="Bold" Margin="0,0,0,2" />
        <TextBox x:Name="NameBox" Grid.Row="1"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,6" />

        <TextBlock x:Name="ErrorText" Grid.Row="2"
                   Text="{Binding ErrorMessage}"
                   Foreground="#FFB00020" TextWrapping="Wrap"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Command="{Binding CancelCommand}"
                    Padding="12,4" Margin="0,0,8,0" IsCancel="True" />
            <Button x:Name="RenameButton" Content="Rename" Command="{Binding ConfirmCommand}"
                    Padding="12,4" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
